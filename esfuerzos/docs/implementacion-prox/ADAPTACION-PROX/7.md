# Paso 7 — Seed del sistema (seed_crisis_bot.py)

Script de arranque idempotente. Provisiona la base de datos con todo lo necesario para que el bot funcione. Seguro de correr en cada deploy.

---

## Archivo creado

**`scripts/seed_crisis_bot.py`**

Ejecuta 5 pasos en orden:

1. `Base.metadata.create_all` — crea todas las tablas si no existen
2. `Operacion` — inserta `nombre="Reúne"`, `slug="reune"`, `waha_session="default"`
3. `BotConfig` — bot activo, `enable_intent_detection=False`, sin horario (24/7 emergencia), `delivery_enabled=False`
4. `FlowTemplate + 8 nodos` — llama `seed_default_flow()`, crea o actualiza los nodos de intake
5. `OperacionFlow` — vincula la Operacion con el FlowTemplate activo

```bash
cd modulos/migration_prox
DATABASE_URL=sqlite:///./test.db python -m scripts.seed_crisis_bot
```

---

## Pendientes

### Migración de columnas en DB existentes

El refactor `Negocio→Operacion` renombró tablas (`negocios→operaciones`, `negocio_flows→operacion_flows`) y la columna `negocio_id→operacion_id`. En bases de datos existentes requiere:

```sql
ALTER TABLE negocios RENAME TO operaciones;
ALTER TABLE negocio_flows RENAME TO operacion_flows;
-- SQLite no soporta RENAME COLUMN; hay que recrear las tablas afectadas.
```

En dev con SQLite basta borrar el `.db` y correr el seed de nuevo.

### Variables de entorno requeridas

El seed solo necesita `DATABASE_URL`. El bot en runtime necesita adicionalmente:
- `WAHA_URL` — endpoint de WAHA
- `WAHA_API_KEY` — si WAHA tiene auth habilitado
- `WAHA_WEBHOOK_URL` — URL pública donde WAHA enviará webhooks

Documentar en `.env.example` antes del primer deploy.

### Seed de operaciones adicionales

El script asume un solo tenant (`slug=reune`). Para múltiples operaciones (ej. coordinaciones regionales) extender el script o crear uno por operación con `WAHA_FREE_TIER=false`.
