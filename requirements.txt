# torch pinned to the CPU wheel: we run InsightFace + SentenceTransformer on CPU, so
# the default CUDA torch (~1.5GB of nvidia wheels) is dead weight that bloated the
# image to 10GB. The CPU wheel keeps it ~3-4GB and builds faster.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu

fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1
apscheduler==3.10.4
slowapi==0.1.9
insightface==0.7.3
onnxruntime==1.18.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
sentence-transformers==3.0.1
rapidfuzz==3.9.7
aiohttp==3.9.5
beautifulsoup4==4.12.3
openpyxl==3.1.5
curl_cffi==0.7.4
